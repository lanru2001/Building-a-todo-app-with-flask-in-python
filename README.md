# Prerequisites:
- Python
- virtualenv
- Flask

# Setup and Configuration:
- mkdir realtime-todo
- cd realtime-todo
- virtualenv .venv
- source .venv/bin/activate

# To install Flask and Pusher:
- pip install flask
- pip install pusher
